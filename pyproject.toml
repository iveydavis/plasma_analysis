[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "plasma_analysis"
version = "0.0.1"
dependencies = [
"numpy>=1.21.6",
"astropy>=5.3.4",
"matplotlib>=3.5.1"]
requires-python = ">=3.9"
authors = [ {name = "Ivey Davis"} ]
readme = "README.md"
